<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Required so sharing keeps publishing (and auto-resumes after a reboot)
         while the app has no visible UI: the boot foreground service only
         receives GPS with "Allow all the time" background location. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Resume location sharing after a reboot / low-battery shutdown. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Scanning is optional; don't exclude camera-less devices from install. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Ntrack"
        android:allowBackup="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|keyboard|uiMode|density|smallestScreenSize"
            android:windowSoftInputMode="adjustResize">
            <!-- NativeActivity loads this library and calls into the
                 android-activity glue, which runs our android_main. -->
            <meta-data
                android:name="android.app.lib_name"
                android:value="ntrack_app" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Tappable group invites: ntrack://join?n=<name>&k=<key> -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ntrack" android:host="join" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ScanActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <service
            android:name=".LocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Serves the rotating GPX export from cacheDir/shared as a
             content:// URI so it can be opened/shared with other apps
             (file:// is illegal across app boundaries on API 24+). Not
             exported; access is granted per-intent via
             FLAG_GRANT_READ_URI_PERMISSION. -->
        <provider
            android:name=".FileBridgeProvider"
            android:authorities="io.ntrack.app.files"
            android:exported="false"
            android:grantUriPermissions="true" />

        <!-- On boot, automatically resumes a share that was active at shutdown,
             or evaluates a pending check-in (gated by the core's resume.flag /
             checkin.flag sentinels), by starting the foreground location
             service headlessly. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
